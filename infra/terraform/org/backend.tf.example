terraform {
  backend "s3" {
    bucket         = "serverless-app-template-org-tf-state"
    key            = "terraform/org.tfstate"
    region         = "us-east-2"
    encrypt        = true
    dynamodb_table = "serverless-app-template-org-tf-locks"
  }
}
